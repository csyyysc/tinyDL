repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8
    hooks:
      - id: clang-format
        args: [--style=file] # Use .clang-format file
        files: \.(cpp|h|cu|cuh)$ # C++, CUDA, and header files